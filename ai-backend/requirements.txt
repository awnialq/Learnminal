# Canonical dependencies: see ../pyproject.toml — install with:
#   uv sync && uv run --directory ai-backend python server.py
adalflow==0.2.*
fastapi==0.104.*
uvicorn==0.24.*
sse-starlette==1.6.*
jinja2==3.1.*
ollama>=0.4
hypothesis==6.*
